

#include <stddef.h>
typedef unsigned int uint8;

// safe_malloc: allocate memory with an error check
void *safe_malloc(size_t);

void rand_arr(char *, uint8);

void set_arr(int, int *, uint8);

int in_arr(int, int *, uint8);
